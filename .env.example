# Database Connection (SQLite in development, PostgreSQL in production)
DATABASE_URL="file:./dev.db"

# JWT Secret for Signed Admin Sessions (64-byte random key in production)
JWT_SECRET="bpcl_super_secret_jwt_key_2026_sapno_ki_sawari"

# QR Station HMAC Secret Key (32-byte random key in production)
QR_HMAC_SECRET="bpcl_qr_hmac_secret_key_2026"

# App Public URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Storage Provider ('local' | 's3')
STORAGE_PROVIDER="local"
S3_BUCKET_NAME="bpcl-campaign-uploads"
AWS_REGION="ap-south-1"

# SMS Gateway Provider ('mock' | 'dlt')
SMS_PROVIDER="mock"
